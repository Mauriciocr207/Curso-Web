<?php include 'includes/header.php';

    // Booleanos
    $logging = true;
    $false = false;
    var_dump($logging, $false);

    // Enteros
    $numero = 200;
    var_dump($numero); 

    // Floats
    $float = 200.5;
    var_dump($float);

    // String
    $nombre = "Juan";
    var_dump($nombre);

    // Arreglos
    $array = [1, "hola", 3.5, false];
    var_dump($array);

include 'includes/footer.php';